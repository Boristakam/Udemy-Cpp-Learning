// int num1 {5};
// int num2 {3};
// num1 = 100; Here we are evaluating the r-value of whatever expression is on the right (100) and storing it in the location of 
// num1 = num2 = 1000 assignment goes from right to left so both variables are 1000

// operations occur on same type operands (use type cast to  ensure this when required otherwise C++ does it for you, can cause unwanted results)
