// ++ and -- increments and decrements to operand by 1 respectively

// prefix ++num    means add 1 before using variable num
// postfix num++   means use variable num and then add 1 to it
// the above only apply for assignment statement like results = ++num1 etc other than that the 2 notations do the same thing to variables regardless of positions

//dont use it twice in the same statement i.e. i++ = ++i      undefined behaviour 