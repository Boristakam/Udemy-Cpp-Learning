expressions:
most basic building block of a progrm
can be a sequence of operands and operators that a specify a computation
compute a value from a number of operands
examples:
34      //literal 
fave_num  //variable
1.3+4.1  //addition
2*5   //multiplication
a>b   //relation (comparison)
a=8   //assignment  c++ is statically typed so it checks that the value on the right hand side can ve assigned to location of the variable on the left

statements:
complete line of code that perform an action and ends whith a semicolomn
usually contains an expression 
examples: null, compound, declaration, try block, selection, eteration statements,if statements and more
int x;  //declaration
num =12; //assignment
1.3+7.4  //expression 
x = 2*6; //multiplication
is a>b cout <<"....

operators:
most are binary therefore need 2 operands e.g. +
unary e.g the negative sign of a number
logical operator e.g. &&, || etc
relational operators e.g. >
incremental operator ++, --